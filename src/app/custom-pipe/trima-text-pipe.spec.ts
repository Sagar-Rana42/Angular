import { TrimaTextPipe } from './trima-text-pipe';

describe('TrimaTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimaTextPipe();
    expect(pipe).toBeTruthy();
  });
});
